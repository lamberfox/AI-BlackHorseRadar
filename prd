📋 软件需求文档 (PRD)：App Store 跨境黑马雷达 — 集成方案

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
一、集成定位
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

本模块不是独立服务，而是在现有 GitHub 黑马雷达基础上新增一条 App Store 分析流水线，
最终产物合并输出到同一张 HTML 报告页（docs/index.html）和同一条 WeCom 通知消息中。

现有系统不变，新增内容：
  - src/app_store_client.py   （新建）App Store 抓取 + 评论斜率过滤
  - src/app_analyzer.py       （新建）App Store 专用 AI 逆向工程分析
  - models.py                 （扩展）新增 AppProject、AppAnalysisResult dataclass
  - reporter.py               （扩展）新增 App Store 区块渲染到同一 HTML 页
  - notifier.py               （扩展）WeCom 消息尾部附加 App Store 摘要
  - main.py                   （扩展）串联 App Store 流水线，任一失败不影响另一条
  - app_snapshots.json        （新建）仓库根目录，快照持久化文件

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
二、整体流程（集成后）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[GitHub Actions 触发]
         │
         ├──── [现有] scraper.py → analyzer.py ────────────────────┐
         │                                                          │
         └──── [新增] app_store_client.py                          │
                    │                                              │
                    ├── 抓取 US/JP 4个分类 RSS                     │
                    ├── 读取 app_snapshots.json                    │
                    ├── 计算斜率，过滤黑马                          │
                    ├── 抓取 30 条五星评论                          │
                    └── app_analyzer.py (DeepSeek)                 │
                              │                                    │
                              ▼                                    ▼
                    [AppAnalysisResult list] + [AnalysisResult list]
                                        │
                                        ▼
                              reporter.py → docs/index.html
                              （GitHub 区块 + App Store 区块，同一页）
                                        │
                                        ▼
                              notifier.py → WeCom（同一条消息）
                                        │
                                        ▼
                              git push app_snapshots.json 回写

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
三、核心数据源配置
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

监控区域：us（美国）、jp（日本）

监控分类：
  6014/7012  Puzzle 益智
  6014/7014  Casual 休闲
  6002       Utilities 工具
  6007       Productivity 生产力

标准接口：
  RSS 新鲜源：
    https://rss.marketingtools.apple.com/api/v2/{region}/apps/new-apps/{limit}/apps.json
  评论数据：
    https://itunes.apple.com/{region}/rss/customerreviews/id={app_id}/sortby=mostrecent/json

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
四、快照持久化结构（app_snapshots.json）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

存放路径：仓库根目录 app_snapshots.json（与 src/ 同级）
每次运行：先读取，运行结束后回写，CI/CD 自动 git push。

结构：
{
  "app_id_xxxxxx": {
    "title": "AppName",
    "region": "us",
    "category": "Puzzle",
    "last_checked": "2026-06-22T12:00:00Z",
    "total_reviews": 45,
    "five_star_reviews": 41,
    "first_seen": "2026-06-18T00:00:00Z"
  }
}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
五、黑马过滤算法（The Black Horse Filters）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

满足以下任一指标即标记为黑马，冻结该 ID 投喂 AI：

指标 A — 新上线爆发流（捕捉冷启动成功的极简 MVP）：
  - (当前时间 - first_seen) ≤ 7 days
  - total_reviews > 10
  - five_star_reviews / total_reviews ≥ 90%

指标 B — 瞬时斜率飙升流（捕捉在社交媒体爆火的既有应用）：
  - ΔReviews = Current_total_reviews - 快照中的 total_reviews
  - ΔReviews ≥ 15 且 ΔTime ≤ 12 hours

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
六、AI 逆向工程模块（app_analyzer.py）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

模型：复用现有 DeepSeek（DEEPSEEK_API_KEY），与 GitHub 分析共享密钥，不引入新密钥。

上下文输入：App 元数据（Name, Description）+ 最新 30 条五星评论文本

System Prompt 视角：
  "准备使用 v0 快速克隆前端、并用 Flutter 还原逻辑的独立开发者"
  禁止套话，直击机制，拒绝营销词汇。

输出 JSON（严格字段，不允许增减）：
{
  "pain_point":       "...",   // 核心吸金痛点：用户 Aha moment 的底层机制
  "v0_prompt":        "...",   // v0.dev 刷图专用纯英文 Prompt，含视觉风格定义
  "flutter_arch":     "...",   // Flutter MVP 架构：Screen 数量 + ChangeNotifier 核心逻辑
  "clone_edge":       "...",   // 截流改进点：五星评论中顺带抱怨的缺陷
  "dark_horse_score": 1        // 1-5 整数，与 GitHub 模块评分体系统一
}

v0_prompt 必须强制包含视觉风格定义：
  3px crisp black outline, high-saturation candy gradients, hard-edged isometric offset shadows

当评论数据不足时，各字段输出"推测依据不足"，不允许捏造。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
七、报告页集成（reporter.py 扩展）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

docs/index.html 在现有两个区块（GitHub Trending / 近3天新星）之后，
追加第三个区块：

  📱 App Store 跨境黑马
  来源：US/JP App Store RSS + iTunes 评论斜率过滤，AI 独立开发者视角逆向

卡片字段（对应 AppAnalysisResult）：
  ⚡ 核心吸金痛点     pain_point
  🎨 v0 刷图指令      v0_prompt（可折叠展示，内容较长）
  🦋 Flutter 架构     flutter_arch
  ✂️ 截流改进点       clone_edge
  评分条              dark_horse_score（与 GitHub 卡片样式统一）

卡片 meta 行显示：区域标签（🇺🇸/🇯🇵）、分类、指标类型（A/B）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
八、WeCom 通知集成（notifier.py 扩展）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

现有消息结构不变，尾部追加 App Store 摘要段：

  📱 App Store 黑马速报
  > 命中 N 个，成功分析 M 个
  > 1. [AppName] (US · Puzzle) ★★★★☆
  > 2. ...

复用现有 WECOM_WEBHOOK_URL，不引入新 Webhook 地址。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
九、新增环境变量
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

变量名               必填  默认值  说明
APP_STORE_REGIONS    否    us,jp   监控区域，逗号分隔
APP_ANALYZE_N        否    5       每次最多投喂 AI 的黑马数量
APP_RSS_LIMIT        否    100     每个分类抓取的 RSS 条目数

（DEEPSEEK_API_KEY 和 WECOM_WEBHOOK_URL 复用现有变量，不重复定义）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
十、CI/CD 变更（.github/workflows/radar.yml）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

新增权限：
  permissions:
    contents: write   # 原为 read，升级以支持快照回写

新增步骤（在 python src/main.py 之后）：
  - name: Commit snapshots
    run: |
      git config user.name  "github-actions[bot]"
      git config user.email "github-actions[bot]@users.noreply.github.com"
      git add app_snapshots.json
      git diff --cached --quiet || git commit -m "chore: update app snapshots [skip ci]"
      git push

[skip ci] 标签防止触发二次 workflow。
快照无变化时（git diff --cached --quiet 为真）跳过 commit，不产生空提交。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
十一、弹性原则（与现有系统统一）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- App Store 整条流水线包裹在 try/except 中；失败只影响 HTML 页的 App Store 区块，
  GitHub 区块和 WeCom 消息主体照常输出。
- 单个 App 分析失败产生降级卡片（显示错误信息），不中断批处理。
- app_store_client.py 每次请求强制 time.sleep(2.0)，重试上限 3 次，超时 10 秒。
- 所有密钥和 Webhook 地址只通过 os.getenv 读取，代码中严禁硬编码。
